services:
  siddhi:
    build: .
    ports:
      - "8080:8080"
    environment:
      ADMIN_PASSWORD: "CHANGE_THIS"
      SECRET_KEY: "CHANGE_THIS_TO_A_LONG_RANDOM_VALUE"
      MAX_UPLOAD_MB: "50"
      PORT: "8080"
    volumes:
      - ./data:/app
